package com.example.algorithm.study;

public class 동적계획법 {

    /**
     *
     * 복잡한 문제를 여러 개의 간단한 문제로 분리하여 부분의 문제들을 해결함으로써
     * 최종적으로 복잡한 문제의 답을 구하는 방법
     *
     * 큰 문제를 작은 문제로 나눌 수 있어야 함
     * 작은 문제들이 반복돼 나타나고 사용, 작은 문제들의 결과값은 항상 같아야 함
     * 모든 작은 문제들은 한 번만 계싼해 DP 테이블에 저장하며 추후 재사용 때 DP 테이블 이용 => 메모이제이션 기법
     * 톱-다운방식, 바텀-업 방식
     *
     * 피보나치 수열 공식: D[n] = D[n-1] + D[n-2]
     *
     *
     */
}
